import React from 'react';

const {
    Provider: ReactShopServiceProvider,
    Consumer: ReactShopServiceConsumer
} = React.createContext();

export {
    ReactShopServiceProvider,
    ReactShopServiceConsumer
};